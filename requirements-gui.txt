# ============================================================
# Dependensi MINIMAL untuk menjalankan GUI main.py (tanpa AI/vision model).
# Cocok untuk: dev cepat, tes GUI, atau mesin tanpa GPU.
# Yang TIDAK di sini (tapi ada di requirements.txt) karena berat/opsional:
#   ultralytics(+torch)  -> hanya perlu kalau GroundSim memakai YOLO asli
#   scikit-fuzzy         -> hanya untuk MockSim fuzzy (opsional, auto-off)
#   pymavlink, pyserial  -> hanya navigator asli / Pixhawk
#   websockets           -> hanya app/gateway.py (dashboard web)
#   keyboard, scipy      -> tooling opsional
# Install:  pip install -r requirements-gui.txt
# ============================================================
PyQt5
PyQtWebEngine
folium
numpy
opencv-python-headless
redis
requests